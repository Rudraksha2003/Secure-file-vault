# Supabase (required for React app and for static HTML pages at build time)
# Get these from your Supabase project: Settings → API
# Do not commit real values; use Netlify env for production.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Netlify functions use these server-side (set in Netlify dashboard, not here):
# SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, ADMIN_USER_ID
